version: 2
updates:
  - package-ecosystem: 'npm'
    directories:
      - '/'
      - '/web'
      - '/backend'
    schedule:
      interval: 'weekly'
    # Match pnpm-workspace.yaml minimumReleaseAge (2880 min = 48h)
    cooldown:
      default-days: 2

  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'weekly'
    cooldown:
      default-days: 2
