version: '3.8'
services:
  tally:
    image: ghcr.io/lukanrocks/tally:latest
    ports:
      - '3000:3000'
    volumes:
      - tally_data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
